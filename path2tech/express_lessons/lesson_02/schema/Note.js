/**
 * import the mongoose package below this comment:
 * const mongoose = require('mongoose');
 */

/**
 * Create a note schema below this comment:
 * const NoteSchema = new mongoose.Schema({...fill this object with the schema properties});
 */

/**
 * Create a note model below this comment:
 * const NoteModel = mongoose.model('Note', NoteSchema);
 */

/**
 * Export the Note model below this comment:
 * module.exports = { NoteModel };
 */