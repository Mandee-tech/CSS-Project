/**
 * import the mongoose package below this comment:
 * const mongoose = require('mongoose');
 */

/**
 * Create a user schema below this comment:
 * const UserSchame = new mongoose.Schema({...fill this object with the schema properties});
 */

/**
 * Create a user model below this comment:
 * const UserModel = mongoose.model('User', UserSchema);
 */

/**
 * Export the User model below this comment:
 * module.exports = { UserModel };
 */