/**
 * You will need to use the following code snippets below this comment:
 * const express = require('express');
 * const cors = require('cors');
 * const app = express();
 * const port = process.env.PORT || 3500;
 */

/**
 * You will need to use the following code snippets below this comment to implement middleware:
 * app.use(cors());
 * app.use(express.json());
 * app.use(express.urlencoded({ extended: true }));
 */

// Create your app.get("/") route below this comment.

// Create your app.get("/notes") route below this comment.

// Create your app.post("/notes") route below this comment.

// Create your app.put("/notes/:id") route below this comment.

// Create your app.delete("/notes/:id") route below this comment.

// Use app.listen() to start the server and have it listen on the port variable below this comment.
